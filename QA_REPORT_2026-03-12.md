# QA Report - Seller Storehouse + Wallet Flow

Date: 2026-03-12
Tester mode: User + QA + Web Developer
Scope: One-click seller catalog import, storehouse order payment, pending balance release on delivery

## Summary
- Status: Partially Passed
- Build: Passed (`npm run build`)
- Targeted lint on changed routes: Passed
- Full workspace lint: Failed due to many pre-existing issues unrelated to this patch
- End-to-end runtime validation against real Supabase data: Not executed from this environment

## What Was Validated
1. Code-path review for seller one-click import flow.
2. Code-path review for checkout `order_items.storehouse_price` population.
3. Code-path review for seller store payment wallet deduction and pending balance booking.
4. Code-path review for delivery transition (pending -> wallet release).
5. Build-time and targeted lint-time validation.

## Results
### PASS
- One-click import endpoint now uses DB-side RPC for large catalogs (11k scale) in `app/api/seller/catalog/import-all/route.ts`.
- Source lineage (`source_product_id`) introduced and used for duplicate detection to avoid title-only collisions.
- Checkout now records `storehouse_price` in `order_items`.
- Store payment now deducts wallet and increases pending balance in one operation.
- Delivery update now releases pending profit into wallet and prevents double-release via `pickup_status` guard.
- New migration adds shop `product_count` sync trigger and backfill logic.

### PASS WITH RISK
- Migration SQL compiles in editor diagnostics, but was not executed against a live database in this run.
- Full user simulation (actual authenticated seller placing and delivering order) cannot be performed without live auth/session + DB test data.

### FAIL (Pre-existing, unrelated to this patch)
- `npm run lint` reports extensive existing issues, including generated/type files and admin pages with `any` usage and TS comment rule violations.
- These failures prevent using full lint as a clean quality gate for this repo right now.

## Missing / Follow-up Items
1. Apply migration: `supabase/migrations/20260312_000023_sync_shop_counts_and_storehouse_prices.sql` in staging/production.
2. Run real seller UAT with a seeded catalog (>= 11k rows) and verify:
   - Import all completes in one click.
   - Imported count/skipped count are sensible.
   - Shop product count updates correctly.
3. Run payment lifecycle UAT:
   - Place order.
   - Pay storehouse from seller side.
   - Confirm wallet decreases and pending increases.
   - Mark delivered.
   - Confirm pending decreases and wallet increases by same released profit.
4. Consider adding DB tests or RPC smoke checks for `seller_import_catalog_products` to prevent regressions.

## Developer Notes
- Full lint currently fails for reasons not introduced by this change; treat as baseline technical debt.
- There is an unrelated deleted file in working tree (`.env.example`) that should be reviewed before release.

## Verdict
- Patch logic is correct for the requested business flow and validated at build + targeted lint level.
- Production readiness depends on applying the migration and completing live UAT on real seller/order data.
